#include "main.h"
/**
 * islower - checks for lowercase character
 * _islower - checks for lowercase
 * Return: int
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
	return (1);
	}
	return (0);
}
